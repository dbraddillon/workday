<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Menu-bar-only agent app: no Dock icon, no app-switcher entry, from the
       very first frame (avoids a Dock-icon flash on launch). The runtime also
       sets ActivationPolicy::Accessory, but this makes it native from t=0. -->
  <key>LSUIElement</key>
  <true/>
</dict>
</plist>
